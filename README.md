# Beauty Salon Booking System

The final project is in `Beauty-Salon-Booking-System/`.

It now follows the same structure as `lesson31-33`:

- `main.py` for FastAPI
- `app.py` for Streamlit
- `database.py` for SQLite setup and seed data
- `models/` for Pydantic models
- `routers/` for API routes
- `auth/` for security helpers

See `Beauty-Salon-Booking-System/README.md` for install and run steps.

The `lesson31-33` patterns were captured in `LESSON31_33_LEARNINGS.md`.
